meta {
  name: CONFIRM - Bereits benutzter Token (400)
  type: http
  seq: 7
}

get {
  url: {{authBaseUrl}}/api/auth/confirm/confirm-verify-used
  auth: none
}

assert {
  res.status: eq 400
}

docs {
  Seed-Token confirm-verify-used (used = true) → 400 Token bereits verwendet.
}
